spring.application.name=paymentservice

server.port=0003

# Database URL (assuming local database and default port)
spring.datasource.url=jdbc:postgresql://localhost:5432/payment

# Database Credentials
spring.datasource.username=postgres
spring.datasource.password=root

# Driver Class (Optional in modern Spring Boot versions)
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update

#Service URLs
service.inventory = http://localhost:0001/api/v1/inventory-service